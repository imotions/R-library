% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imotionsApi.R
\name{getTouchActors}
\alias{getTouchActors}
\title{Get all touch actors defined for a specific stimulus.}
\usage{
getTouchActors(study, stimulus)
}
\arguments{
\item{study}{An imStudy object as returned from \code{\link{imStudy}}.}

\item{stimulus}{An imStimulus object as returned from \code{\link{getStimuli}}.}
}
\value{
An imTouchActorList object (data.table) with one row per touch actor, or NULL if none are defined.
}
\description{
A touch actor detects hand-to-object contact on a recorded video and marks a set of AOIs as interactive. Contact
detection runs in the iMotions desktop application only, so this returns NULL for a remote study.
}
\examples{
\dontrun{
connection <- imotionsApi::imConnection("xxxxxxxx")
studies <- imotionsApi::listStudies(connection)
study <- imotionsApi::imStudy(connection, studies$id[1])
stimuli <- imotionsApi::getStimuli(study)
touchActors <- imotionsApi::getTouchActors(study, stimuli[1, ])
}
}
