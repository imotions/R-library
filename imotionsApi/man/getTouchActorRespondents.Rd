% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imotionsApi.R
\name{getTouchActorRespondents}
\alias{getTouchActorRespondents}
\title{Get the respondents that have contact data for a specific stimulus.}
\usage{
getTouchActorRespondents(study, stimulus, segment = NULL)
}
\arguments{
\item{study}{An imStudy object as returned from \code{\link{imStudy}}.}

\item{stimulus}{An imStimulus object as returned from \code{\link{getStimuli}}.}

\item{segment}{Optional - an imSegment object as returned from \code{\link{getSegments}} to filter respondents.}
}
\value{
An imRespondentList object (data.table) with the respondents that have contact data.
}
\description{
\code{\link{getRespondents}} cannot be used with an AOI here: its imAOI method resolves respondents through the gaze
in/out endpoint, which returns nothing on a study without eyetracking data - exactly the case contact metrics exist
to support. This filters the stimulus's respondents by whether contact in/out signals exist for them instead.
}
\examples{
\dontrun{
connection <- imotionsApi::imConnection("xxxxxxxx")
studies <- imotionsApi::listStudies(connection)
study <- imotionsApi::imStudy(connection, studies$id[1])
stimulus <- imotionsApi::getStimuli(study)[1, ]
respondents <- imotionsApi::getTouchActorRespondents(study, stimulus)
}
}
