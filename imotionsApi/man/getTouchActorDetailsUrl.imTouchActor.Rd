% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imotionsApi.R
\name{getTouchActorDetailsUrl.imTouchActor}
\alias{getTouchActorDetailsUrl.imTouchActor}
\title{getTouchActorDetailsUrl.imTouchActor method to return the path/url to one touch actor's contact in/out details,
across every AOI it marks interactive.}
\usage{
\method{getTouchActorDetailsUrl}{imTouchActor}(study, imObject, respondent = NULL)
}
\arguments{
\item{study}{An imStudy object as returned from \code{\link{imStudy}}.}

\item{imObject}{An imStimulus object, an imTouchActor object as returned from \code{\link{getTouchActors}}, or one
row of \code{\link{getTouchActorAois}}.}

\item{respondent}{Optional - An imRespondent object as returned from \code{\link{getRespondents}}. Without one,
the url targets every respondent exposed to the stimulus in a single response.}
}
\description{
Produces the same url as \code{\link{getTouchActorDetailsUrl.imTouchAOI}} for the same actor - the endpoint is
scoped per touch actor either way, so which of the two dispatches only depends on whether the caller already
narrowed down to one (touch actor, AOI) pair or still has the bare touch actor.
}
\keyword{internal}
